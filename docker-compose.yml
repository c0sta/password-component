version: "3"

services:
  app:
    build: .
    entrypoint: sh -c "npm install && npm run dev"
    volumes:
      - .:/app
    ports:
      - 3000:3000
  storybook:
    build: .
    entrypoint: sh -c "npm install && npm run storybook"
    volumes:
      - .:/app
    ports:
      - 6006:6006
